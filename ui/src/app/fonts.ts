import { Inter, Lalezar } from 'next/font/google';

export const lalezar = Lalezar({
  weight: '400',
  subsets: ['latin'],
});

export const inter = Inter({
  weight: 'variable',
  subsets: ['latin'],
});
